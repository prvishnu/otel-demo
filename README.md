This project tests open-telemetry logging capabilities with Spring boot. It demonstrates the concepts of traceId, spanId and propagation

Build: mvn clean install
To start the project: ./run.bat
To test the project using curl command: ./test.bat
